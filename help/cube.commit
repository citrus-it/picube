NAME		cube.commit
SYNTAX		cube.commit [-nocopy]
DESCRIPTION	Finish building a cube in a temporary buffer.
		Without the optional -nocopy argument, the buffered cube
		will be copied across to the real one, otherwise it will
		stay in the buffer.
RETURN VALUE	None
SEE ALSO	cube.begin
